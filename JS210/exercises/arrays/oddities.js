// Oddities

// oh... this question is just trivia
// This is because JavaScript compares arrays (/objects) via reference, not by value.
// because an array literal is being used to compare, it, by definition, cannot
// be the same array as the result of the function call.
