// 1
// Strings should be within ''

// 2
// No multiple assigments in a single statement.
// Last Variable uses wrong naming convention.

// 3
// Should use `const` for variable assignment and surround expression with parentheses.
// Missed the strict equality operator, though I already knew that bit.

// 4
// If statements without blocks should be on a single line.

// 5
// Improper indentation. Should use two spaces per level.
// Use function expressions instead of function declarations.
// No spaces surrounding `=` and `<` in the for loop constructor.
// `let` used instead of `const` for a variable which will not be reassigned.
// Implicit coercion and manual string concatenation are used here in the variable declaration.
